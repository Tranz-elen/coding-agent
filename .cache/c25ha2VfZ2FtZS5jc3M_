/* CSS变量定义 */
:root {
    --theme-bg: #1a1a2e;
    --theme-secondary: #16213e;
    --theme-snake-head: #00dbde;
    --theme-snake-body: #4cd137;
    --theme-food: #ff3838;
    --theme-text: #ffffff;
    --theme-accent: #fc00ff;
}

/* 重置样式 */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, var(--theme-bg) 0%, var(--theme-secondary) 100%);
    color: var(--theme-text);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    transition: all 0.5s ease;
}

.game-container {
    max-width: 800px;
    width: 100%;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.5s ease;
}

/* 头部样式 */
.game-header {
    text-align: center;
    margin-bottom: 30px;
}

.game-header h1 {
    font-size: 2.5rem;
    margin-bottom: 10px;
    background: linear-gradient(45deg, var(--theme-snake-head), var(--theme-accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.game-description {
    color: #b0b0b0;
    font-size: 1.1rem;
    max-width: 600px;
    margin: 0 auto;
}

/* 游戏信息区域 */
.game-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    flex-wrap: wrap;
    gap: 20px;
}

.score-board {
    display: flex;
    gap: 30px;
    background: rgba(0, 0, 0, 0.3);
    padding: 15px 25px;
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.score-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.2rem;
}

.score-item i {
    color: var(--theme-snake-head);
    font-size: 1.5rem;
}

.controls {
    display: flex;
    gap: 15px;
}

.btn {
    padding: 12px 25px;
    border: none;
    border-radius: 10px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 8px;
}

.btn-primary {
    background: linear-gradient(45deg, #00b09b, #96c93d);
    color: white;
}

.btn-secondary {
    background: linear-gradient(45deg, #8a2387, #f27121);
    color: white;
}

.btn-danger {
    background: linear-gradient(45deg, #ff416c, #ff4b2b);
    color: white;
}

.btn-sound {
    background: linear-gradient(45deg, #3498db, #2980b9);
    color: white;
}

.btn-sound.muted {
    background: linear-gradient(45deg, #95a5a6, #7f8c8d);
}

.btn-warning {
    background: linear-gradient(45deg, #f39c12, #e67e22);
    color: white;
}

.btn-info {
    background: linear-gradient(45deg, #3498db, #2980b9);
    color: white;
}

.btn-theme {
    background: linear-gradient(45deg, #9b59b6, #8e44ad);
    color: white;
}

.difficulty-selector {
    display: flex;
    align-items: center;
}

#difficulty-select {
    padding: 12px 20px;
    border: none;
    border-radius: 10px;
    background: linear-gradient(45deg, #9b59b6, #8e44ad);
    color: white;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    min-width: 120px;
}

#difficulty-select:hover {
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

#difficulty-select option {
    background: #2c3e50;
    color: white;
}

.btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

.btn:active {
    transform: translateY(0);
}

/* 游戏区域 */
.game-area {
    position: relative;
    margin-bottom: 30px;
}

#game-canvas {
    display: block;
    background: #0f1525;
    border-radius: 15px;
    border: 3px solid var(--theme-snake-head);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
}

.game-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(15, 21, 37, 0.95);
    display: flex;
    justify-content: center;
    align-items: center;
    border-radius: 15px;
    z-index: 10;
}

.overlay-content {
    text-align: center;
    padding: 40px;
    max-width: 500px;
}

.overlay-content h2 {
    font-size: 2.5rem;
    margin-bottom: 20px;
    color: var(--theme-snake-head);
}

.overlay-content p {
    font-size: 1.3rem;
    margin-bottom: 30px;
    color: #b0b0b0;
}

.instructions {
    text-align: left;
    background: rgba(0, 0, 0, 0.3);
    padding: 20px;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.instructions h3 {
    color: var(--theme-accent);
    margin-bottom: 15px;
    display: flex;
    align-items: center;
    gap: 10px;
}

.instructions ul {
    list-style: none;
}

.instructions li {
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    gap: 10px;
    color: #b0b0b0;
}

.instructions li i {
    color: var(--theme-snake-head);
    width: 20px;
}

/* 键盘控制 */
.keyboard-controls {
    text-align: center;
    margin-bottom: 30px;
}

.keyboard-controls h3 {
    margin-bottom: 20px;
    color: var(--theme-snake-head);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
}

.key-grid {
    display: inline-block;
    background: rgba(0, 0, 0, 0.3);
    padding: 20px;
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.key-row {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-bottom: 15px;
}

.key {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #2d3436, #636e72);
    border-radius: 10px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.5rem;
    font-weight: bold;
    color: white;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    transition: all 0.2s ease;
    user-select: none;
}

.key.active {
    background: linear-gradient(135deg, var(--theme-snake-head), var(--theme-snake-body));
    transform: scale(1.1);
    box-shadow: 0 0 20px var(--theme-snake-head);
}

/* 浮动动画 */
@keyframes floatUp {
    0% {
        opacity: 1;
        transform: translateY(0);
    }
    100% {
        opacity: 0;
        transform: translateY(-50px);
    }
}

/* 触摸反馈动画 */
@keyframes touchFeedback {
    0% {
        opacity: 1;
        transform: scale(1);
    }
    100% {
        opacity: 0;
        transform: scale(1.5);
    }
}

/* 得分动画 */
@keyframes scoreFloat {
    0% {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
    100% {
        opacity: 0;
        transform: translateY(-50px) scale(1.2);
    }
}

.score-float {
    position: absolute;
    font-size: 1.2rem;
    font-weight: bold;
    color: #ffd700;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
    pointer-events: none;
    z-index: 100;
    animation: scoreFloat 1s ease-out forwards;
}

/* 食物生成动画 */
@keyframes foodSpawn {
    0% {
        transform: scale(0);
        opacity: 0;
    }
    70% {
        transform: scale(1.2);
        opacity: 1;
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}

.food-spawn {
    animation: foodSpawn 0.3s ease-out;
}

/* 蛇身增长动画 */
@keyframes snakeGrow {
    0% {
        transform: scale(0.8);
    }
    100% {
        transform: scale(1);
    }
}

.snake-grow {
    animation: snakeGrow 0.2s ease-out;
}

/* 游戏统计 */
.game-stats {
    margin-top: 30px;
    background: rgba(0, 0, 0, 0.3);
    padding: 20px;
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.game-stats h3 {
    color: var(--theme-snake-head);
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 10px;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 15px;
}

.stat-item {
    background: rgba(255, 255, 255, 0.05);
    padding: 15px;
    border-radius: 10px;
    text-align: center;
    transition: all 0.3s ease;
}

.stat-item:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-3px);
}

.stat-label {
    font-size: 0.9rem;
    color: #b0b0b0;
    margin-bottom: 8px;
}

.stat-value {
    font-size: 1.5rem;
    font-weight: bold;
    color: var(--theme-snake-head);
}

/* 触摸控制按钮 */
.touch-controls {
    display: none;
    position: absolute;
    bottom: 20px;
    right: 20px;
    z-index: 20;
}

.touch-controls-grid {
    display: grid;
    grid-template-areas:
        ". up ."
        "left . right"
        ". down .";
    gap: 10px;
}

.touch-control {
    width: 60px;
    height: 60px;
    background: rgba(var(--theme-snake-head-rgb), 0.7);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    font-weight: bold;
    color: white;
    cursor: pointer;
    user-select: none;
    transition: all 0.2s ease;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
}

.touch-control.active {
    background: var(--theme-snake-head);
    transform: scale(1.1);
    box-shadow: 0 0 20px var(--theme-snake-head);
}

.touch-control.up {
    grid-area: up;
}

.touch-control.down {
    grid-area: down;
}

.touch-control.left {
    grid-area: left;
}

.touch-control.right {
    grid-area: right;
}

/* 移动设备适配 */
@media (max-width: 768px) and (hover: none) and (pointer: coarse) {
    .touch-controls {
        display: block;
    }
    
    .keyboard-controls {
        display: none;
    }
    
    .controls {
        flex-wrap: wrap;
        justify-content: center;
    }
    
    .btn {
        padding: 10px 15px;
        font-size: 1rem;
        min-width: 120px;
    }
    
    .score-board {
        padding: 10px 15px;
        gap: 15px;
    }
    
    .score-item {
        font-size: 1rem;
    }
}

/* 页脚 */
.game-footer {
    text-align: center;
    padding-top: 20px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    color: #b0b0b0;
    font-size: 0.9rem;
}

/* 响应式设计 */
@media (max-width: 768px) {
    .game-container {
        padding: 20px;
    }
    
    .game-header h1 {
        font-size: 2rem;
    }
    
    .game-info {
        flex-direction: column;
        align-items: stretch;
    }
    
    .score-board {
        justify-content: center;
        flex-wrap: wrap;
    }
    
    .controls {
        justify-content: center;
    }
    
    #game-canvas {
        width: 100%;
        height: auto;
    }
    
    .key {
        width: 50px;
        height: 50px;
        font-size: 1.2rem;
    }
}

@media (max-width: 480px) {
    .game-header h1 {
        font-size: 1.8rem;
    }
    
    .score-item {
        font-size: 1rem;
    }
    
    .btn {
        padding: 10px 20px;
        font-size: 1rem;
    }
    
    .key {
        width: 40px;
        height: 40px;
        font-size: 1rem;
    }
}